# IntegrityIndex.ca — Production deployment
# Copy to .env and fill in values. Never commit .env.

# Database (Supabase)
# Use pooled connection (port 6543) for the app to avoid exhausting connections.
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection (port 5432) for Prisma migrations and introspect only.
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# Optional: Finnhub API key for live stock prices on the ticker
FINNHUB_API_KEY="your-finnhub-api-key"

# Required for Vercel Cron: secret sent as Authorization: Bearer <CRON_SECRET>
CRON_SECRET="your-cron-secret-at-least-16-chars"
