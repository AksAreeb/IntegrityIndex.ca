-- Run this in Supabase SQL Editor (Dashboard → SQL Editor) BEFORE running prisma migrate dev.
-- Creates the shadow database required by Prisma Migrate when automatic creation is not allowed.

CREATE DATABASE shadow;
